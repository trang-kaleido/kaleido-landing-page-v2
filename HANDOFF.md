# Handoff

Append a 1–2 sentence note after each session. Most recent first.

---

## 2026-05-13
Added language-router architecture: `/` is now a paper-background language selector (hex SVG mark, stacked EN/VI choices, ghost-text IELTS questions in bg). English landing moved to `/en/`; Vietnamese stays at `/vi/`. No `vercel.json` changes needed — `cleanUrls: true` handles routing.
